import { ModulePlaceholderDirective } from './module-placeholder.directive';

describe('ModulePlaceholderDirective', () => {
  it('should create an instance', () => {
    const directive = new ModulePlaceholderDirective();
    expect(directive).toBeTruthy();
  });
});
